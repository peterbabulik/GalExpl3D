// mining-animation.ts

// DEPRECATED: This file's logic has been replaced by a UI-based timer
// in UI.tsx (MiningProgressIndicator) and App.tsx. The functions are kept
// empty to prevent breaking existing imports but no longer do anything.

import type * as THREE from 'three';

export function startMiningAnimation(scene: THREE.Scene, source: THREE.Object3D, target: THREE.Object3D) {
    // Deprecated
}

export function updateMiningAnimation(source: THREE.Object3D, target: THREE.Object3D) {
    // Deprecated
}

export function stopMiningAnimation(scene: THREE.Scene) {
    // Deprecated
}